import { createTheme } from "@mui/material/styles";

export const themeMaterialUI = createTheme({
  palette: {
    background: {
      default: "#313233",
      paper: "#ffffff",
    },
  },
});
